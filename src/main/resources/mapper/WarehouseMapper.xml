<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.WarehouseMapper">

    <resultMap id="BaseResultMap" type="com.example.shopeeerp.pojo.Warehouse">
        <id column="warehouse_id" property="warehouseId" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="location" property="location" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        warehouse_id, name, location
    </sql>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.Warehouse" useGeneratedKeys="true" keyProperty="warehouseId">
        INSERT INTO warehouses (name, location)
        VALUES (#{name}, #{location})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM warehouses WHERE warehouse_id = #{warehouseId}
    </delete>

    <update id="update" parameterType="com.example.shopeeerp.pojo.Warehouse">
        UPDATE warehouses
        SET name = #{name},
            location = #{location}
        WHERE warehouse_id = #{warehouseId}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM warehouses
        WHERE warehouse_id = #{warehouseId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM warehouses
    </select>

</mapper>
