<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.CustomerSupportMapper">

    <resultMap id="BaseResultMap" type="com.example.shopeeerp.pojo.CustomerSupport">
        <id column="support_id" property="supportId" jdbcType="BIGINT"/>
        <result column="customer_id" property="customerId" jdbcType="BIGINT"/>
        <result column="issue_description" property="issueDescription" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        support_id, customer_id, issue_description, status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.CustomerSupport" useGeneratedKeys="true" keyProperty="supportId">
        INSERT INTO customer_support (customer_id, issue_description, status, created_at, updated_at)
        VALUES (#{customerId}, #{issueDescription}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM customer_support WHERE support_id = #{supportId}
    </delete>

    <update id="update" parameterType="com.example.shopeeerp.pojo.CustomerSupport">
        UPDATE customer_support
        SET customer_id = #{customerId},
            issue_description = #{issueDescription},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE support_id = #{supportId}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer_support
        WHERE support_id = #{supportId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer_support
    </select>

    <select id="selectByCustomerId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer_support
        WHERE customer_id = #{customerId}
    </select>

</mapper>
