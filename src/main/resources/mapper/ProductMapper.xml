<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.example.shopeeerp.pojo.Product">
        <id column="product_id" property="productId" jdbcType="BIGINT"/>
        <result column="sku" property="sku" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="category_id" property="categoryId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        product_id, sku, name, description, price, category_id, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.Product" useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO products 
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productId != null">product_id,</if>
            sku, name, description, price, category_id, created_at, updated_at
        </trim>
        VALUES 
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productId != null">#{productId},</if>
            #{sku}, #{name}, #{description}, #{price}, #{categoryId}, #{createdAt}, #{updatedAt}
        </trim>
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM products WHERE product_id = #{productId}
    </delete>

    <update id="update" parameterType="com.example.shopeeerp.pojo.Product">
        UPDATE products
        SET sku = #{sku},
            name = #{name},
            description = #{description},
            price = #{price},
            category_id = #{categoryId},
            updated_at = #{updatedAt}
        WHERE product_id = #{productId}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
        WHERE product_id = #{productId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
    </select>

    <select id="selectBySku" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
        WHERE sku = #{sku}
    </select>

    <select id="selectByCategoryId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
        WHERE category_id = #{categoryId}
    </select>

</mapper>
