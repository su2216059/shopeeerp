<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.shopeeerp.pojo.User">
        <id column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, password, role_id, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, role_id, created_at, updated_at)
        VALUES (#{username}, #{password}, #{roleId}, #{createdAt}, #{updatedAt})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

    <update id="update" parameterType="com.example.shopeeerp.pojo.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            role_id = #{roleId},
            updated_at = #{updatedAt}
        WHERE user_id = #{userId}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectByRoleId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE role_id = #{roleId}
    </select>

</mapper>
