<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.RolePermissionMapper">

    <resultMap id="RolePermissionResultMap" type="com.example.shopeeerp.pojo.RolePermission">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="permission_id" property="permissionId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.RolePermission" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role_permissions (role_id, permission_id, created_at)
        VALUES (#{roleId}, #{permissionId}, NOW())
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM role_permissions WHERE role_id = #{roleId}
    </delete>

    <select id="selectByRoleId" resultMap="RolePermissionResultMap">
        SELECT id, role_id, permission_id, created_at
        FROM role_permissions
        WHERE role_id = #{roleId}
        ORDER BY id ASC
    </select>

</mapper>
