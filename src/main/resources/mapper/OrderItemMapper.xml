<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopeeerp.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.example.shopeeerp.pojo.OrderItem">
        <id column="order_item_id" property="orderItemId" jdbcType="BIGINT"/>
        <result column="order_id" property="orderId" jdbcType="BIGINT"/>
        <result column="product_id" property="productId" jdbcType="BIGINT"/>
        <result column="quantity" property="quantity" jdbcType="INTEGER"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="total" property="total" jdbcType="DECIMAL"/>
    </resultMap>

    <sql id="Base_Column_List">
        order_item_id, order_id, product_id, quantity, price, total
    </sql>

    <insert id="insert" parameterType="com.example.shopeeerp.pojo.OrderItem" useGeneratedKeys="true" keyProperty="orderItemId">
        INSERT INTO order_items (order_id, product_id, quantity, price, total)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{price}, #{total})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM order_items WHERE order_item_id = #{orderItemId}
    </delete>

    <update id="update" parameterType="com.example.shopeeerp.pojo.OrderItem">
        UPDATE order_items
        SET order_id = #{orderId},
            product_id = #{productId},
            quantity = #{quantity},
            price = #{price},
            total = #{total}
        WHERE order_item_id = #{orderItemId}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM order_items
        WHERE order_item_id = #{orderItemId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM order_items
    </select>

    <select id="selectByOrderId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM order_items
        WHERE order_id = #{orderId}
    </select>

    <select id="selectByProductId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM order_items
        WHERE product_id = #{productId}
    </select>

</mapper>
