订单模块：订单同步、订单列表查询（支持筛选/分页）

商品模块：商品同步、商品列表查询（支持筛选/分页）

规划中：利润计算（按订单/按 SKU 维度）、利润报表（日/周/月）

4. 已设计：利润查询 DTO + 返回体 + Service 接口
5. 已确认：profit_order / profit_daily_summary 表结构（MySQL 8.0）
6. 成本口径：采购价
“数据量小时优化器可能选择不同索引，不代表慢；数据增大后会更倾向 shop_id + biz_date 的 range 索引，必要时可用 FORCE INDEX 固化报表查询计划。”

